# Triatleta 24h — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Запустить live-результаты суточной велогонки Triatleta на `live-race.triatleta.ru` к старту гонки 25.06.2026 20:00 Красноярск.

**Architecture:** Новый модуль в существующем FastAPI-приложении. nginx маршрутизирует `live-race.triatleta.ru` → порт 8000; FastAPI определяет Host-заголовок и отдаёт Tri-страницы. Отдельная БД `triatleta_24h` на том же MySQL VPS. Новый загрузчик `load_tri_results.py` опрашивает Copernico каждые 30 сек.

**Tech Stack:** FastAPI, MySQL (mysql-connector-python), Jinja2, nginx, Let's Encrypt, systemd, Copernico API

---

## Файловая карта

| Файл | Действие | Назначение |
|------|----------|-----------|
| `config/copernico/tri_24h_2026.yaml` | Создать | Copernico пресет (поля + паттерн кругов) |
| `config/events/tri_24h.yaml` | Создать | Конфиг события |
| `config/loader/tri_24h.env` | Создать | Параметры загрузчика |
| `load_tri_results.py` | Создать | Загрузчик: Copernico → triatleta_24h |
| `deploy/run_tri_loader.sh` | Создать | Shell-обёртка для systemd |
| `deploy/km_tri_loader@.service` | Создать | Systemd service template |
| `deploy/nginx.conf` | Изменить | Добавить server block для live-race.triatleta.ru |
| `src/triatleta/__init__.py` | Создать | Пакет |
| `src/triatleta/db.py` | Создать | Соединение с triatleta_24h |
| `src/triatleta/service.py` | Создать | Расчёт таблицы: круги, км, скорость, отставание |
| `src/triatleta/router.py` | Создать | FastAPI роуты: GET /, GET /api/tri/standings, GET /api/tri/laps |
| `src/tracker/router.py` | Изменить | Подключить triatleta router |
| `templates/tri_results.html` | Создать | Live-результаты: polling + сплиты |
| `static/css/tri_results.css` | Создать | Стили |
| `tests/unit/test_tri_service.py` | Создать | Unit-тесты расчёта standings |

| `seed_tri_test_data.py` | Создать | Заполнение БД тестовыми данными (без Copernico) |

---

## Task 0: Верификация пресета Copernico

**Статус: ВЫПОЛНЕНО 2026-06-23** — поля верифицированы, паттерн кругов подтверждён по UI.

**Итог верификации:**
- `dorsal` — поле номера (не `bib`) ✅
- `gender` возвращает `'male'`/`'female'` → загрузчик нормализует в `M`/`F` ✅
- Паттерн кругов `times.official_{n}kr` — **подтверждён по колонкам UI** (`1kr`, `2kr`...) ✅
- В Copernico UI пресет ограничен диапазоном **1–100 кругов** (max в интерфейсе)
- В гонке настроено **173 КТ** (одна физическая точка). YAML count=173
- **⚠️ Риск:** топ-велосипедисты могут сделать 130–148 кругов. Поля 101–173 могут не вернуться при пресете 1–100

- [x] Создать пресет в Copernico UI — **выполнено**
- [x] Верифицировать поля через API — **выполнено**
- [x] Исправить `bib → dorsal`, нормализовать gender, добавить team_name — **выполнено**
- [ ] **Шаг Post-start: Проверить 101+ кругов сразу после старта**

  Выполнить через ~1.5–2ч после старта (когда лидеры пройдут 10–15 кругов):
  ```bash
  curl "https://public-api.copernico.cloud/api/races/triatleta-sutochnaya-velogonka-24-chasa-2026/preset/podbor250718@gmail.com:::tri_24h_2026/24h%20race" \
    | python -m json.tool | findstr "times"
  ```
  Проверить: есть ли в ответе `times.official_1kr`, `times.official_2kr` и т.д.

  **Если API возвращает все круги → ничего делать не надо.**

  **Если API режет на 100:** создать второй пресет `tri_24h_2026_b` в Copernico UI (диапазон 101–173) и обновить загрузчик:

  ```python
  # load_tri_results.py — добавить второй API-вызов если count > 100:
  # runners_b = _fetch_copernico(race_id, login, "tri_24h_2026_b", event)
  # Смерджить по dorsal, добавить поля lap_101...lap_173
  ```

- [ ] **Шаг Post-start: Шаблон второго пресета (если нужен)**

  Создать `config/copernico/tri_24h_2026_b.yaml`:
  ```yaml
  description: "Triatleta 24h 2026. Круги 101-173 (второй пресет)."
  lap_fields:
    count: 173
    offset: 100       # загрузчик читает с 101-го
    pattern: "times.official_{n}kr"
  ```

---

## Task 0b: УСТАРЕЛО — ниже оригинальные шаги (архив)

- [ ] **Шаг 1: Создать пресет в Copernico UI**

  Войти на copernico.cloud → найти гонку → создать пресет `tri_24h_2026` с нужными полями:
  - Номер участника, ФИО, категория, пол
  - Времена кругов (узнать точные названия полей)

- [ ] **Шаг 2: Запросить сырой ответ API**

```bash
# Открыть в браузере или curl:
curl "https://public-api.copernico.cloud/api/races/triatleta-sutochnaya-velogonka-24-chasa-2026/preset/podbor250718@gmail.com:::tri_24h_2026/24h%20race" | python -m json.tool | head -80
```

  Ожидать: JSON-массив объектов с ключами участника. Пример ожидаемого объекта:
  ```json
  {
    "dorsal": 1,          // ← может быть "bib"!
    "surname": "Иванов",
    "name": "Алексей",
    "gender": "M",
    "category": "individual",
    "times.official_1kr": 555000,   // ← паттерн кругов
    "times.official_2kr": 1110000,
    ...
  }
  ```

- [ ] **Шаг 3: Обновить пресет под реальные поля**

  Если поля отличаются — исправить `config/copernico/tri_24h_2026.yaml`:
  ```yaml
  fields:
    start_number: dorsal   # если это "dorsal", а не "bib"
    surname: surname
    name: name
    birthdate: birthdate
    gender: gender
    status: status
    category: category
  ```

- [ ] **Шаг 4: Закоммитить исправление пресета**

```bash
git add config/copernico/tri_24h_2026.yaml
git commit -m "fix: tri_24h copernico preset — verified field names from real API"
```

---

## Task 1: БД `triatleta_24h` на VPS

**Files:** только SQL на VPS (ничего в репозитории)

- [ ] **Шаг 1: Подключиться к VPS**

```bash
ssh root@89.108.88.104
mysql -u km_analytic -p  # пароль из .env: DB_PASSWORD
```

- [ ] **Шаг 2: Создать БД и таблицы**

```sql
CREATE DATABASE IF NOT EXISTS triatleta_24h CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE triatleta_24h;

CREATE TABLE events (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    gun_datetime DATETIME NOT NULL,
    lap_distance_km DECIMAL(5,3) NOT NULL,
    duration_hours INT NOT NULL
);

CREATE TABLE participants (
    id INT AUTO_INCREMENT PRIMARY KEY,
    event_id INT NOT NULL,
    start_number INT NOT NULL,
    surname VARCHAR(255) NOT NULL,
    name VARCHAR(255) NOT NULL,
    birthdate DATE,
    gender VARCHAR(10),
    status VARCHAR(50),
    category VARCHAR(100),
    team_name VARCHAR(255),
    UNIQUE KEY uniq_event_bib (event_id, start_number)
);

CREATE TABLE laps (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    participant_id INT NOT NULL,
    event_id INT NOT NULL,
    lap_number INT NOT NULL,
    cumulative_ms BIGINT NOT NULL,
    lap_ms BIGINT NOT NULL,
    UNIQUE KEY uniq_participant_lap (participant_id, lap_number),
    INDEX idx_event_lap (event_id, lap_number)
);
```

- [ ] **Шаг 3: Вставить запись события и запомнить id**

```sql
INSERT INTO events (code, name, gun_datetime, lap_distance_km, duration_hours)
VALUES ('tri_24h_2026', 'Triatleta — Суточная велогонка 24ч', '2026-06-25 13:00:00', 4.040, 24);

SELECT id FROM events WHERE code = 'tri_24h_2026';
-- Запомни этот id (скорее всего = 1)
```

- [ ] **Шаг 4: Дать права km_analytic на новую БД**

```sql
GRANT ALL PRIVILEGES ON triatleta_24h.* TO 'km_analytic'@'%';
GRANT ALL PRIVILEGES ON triatleta_24h.* TO 'km_analytic'@'localhost';
FLUSH PRIVILEGES;
```

---

## Task 2: Конфиг-файлы

**Files:**
- Create: `config/copernico/tri_24h_2026.yaml`
- Create: `config/events/tri_24h.yaml`
- Create: `config/loader/tri_24h.env`

- [ ] **Шаг 1: Создать Copernico пресет**

```yaml
# config/copernico/tri_24h_2026.yaml
description: "Triatleta 24h 2026. 150 кругов по 4.04 км. КТ: 1kr..150kr."

fields:
  start_number: bib
  surname: surname
  name: name
  birthdate: birthdate
  gender: gender
  status: status
  category: category

lap_fields:
  count: 150
  pattern: "times.official_{n}kr"
```

- [ ] **Шаг 2: Создать конфиг события** (подставить id из Task 1 Шаг 3)

```yaml
# config/events/tri_24h.yaml
name: "Triatleta 24h"
code: "tri_24h_2026"
year: 2026
gun_time: "13:00:00"

distances:
  - distance: "24h"
    db_event_id: 1        # id из triatleta_24h.events
    copernico:
      race_id: "triatleta-sutochnaya-velogonka-24-chasa-2026"
      login: "podbor250718@gmail.com"
      preset: "tri_24h_2026"
      event: "24h race"   # название ивента в Copernico
```

- [ ] **Шаг 3: Создать конфиг загрузчика**

```
# config/loader/tri_24h.env
LOADER_CONFIG=config/events/tri_24h.yaml
LOADER_INTERVAL=30
```

- [ ] **Шаг 4: Закоммитить**

```bash
git add config/copernico/tri_24h_2026.yaml config/events/tri_24h.yaml config/loader/tri_24h.env
git commit -m "feat: triatleta 24h — copernico preset and event config"
```

---

## Task 3: Загрузчик `load_tri_results.py`

**Files:**
- Create: `load_tri_results.py`
- Create: `deploy/run_tri_loader.sh`
- Create: `deploy/km_tri_loader@.service`

- [ ] **Шаг 1: Создать загрузчик**

```python
# load_tri_results.py
"""Загрузчик результатов суточной велогонки Triatleta из Copernico API."""
import argparse
import logging
import os
import time
from pathlib import Path
from typing import Optional

import mysql.connector
import urllib.request
import urllib.parse
import json
import yaml
from dotenv import load_dotenv

load_dotenv()

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)-8s] %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
)
logger = logging.getLogger("TriLoader")

LAP_DISTANCE_KM = 4.040


def _connect() -> mysql.connector.MySQLConnection:
    return mysql.connector.connect(
        host=os.getenv("DB_HOST", "127.0.0.1"),
        port=int(os.getenv("DB_PORT", "3306")),
        database="triatleta_24h",
        user=os.getenv("DB_USER", "km_analytic"),
        password=os.getenv("DB_PASSWORD"),
        charset="utf8mb4",
        autocommit=False,
    )


def _fetch_copernico(race_id: str, login: str, preset: str, event: str) -> list:
    encoded_preset = urllib.parse.quote(preset)
    encoded_event = urllib.parse.quote(event)
    url = f"https://public-api.copernico.cloud/api/races/{race_id}/preset/{login}:::{encoded_preset}/{encoded_event}"
    logger.info(f"📡 Copernico: {url}")
    req = urllib.request.Request(url, headers={"Accept": "application/json"})
    with urllib.request.urlopen(req, timeout=90) as resp:
        data = json.loads(resp.read().decode("utf-8"))
    if isinstance(data, list):
        return data
    return data.get("results", data.get("data", []))


def _load_config(config_path: str, distance: str) -> dict:
    with open(config_path, encoding="utf-8") as f:
        cfg = yaml.safe_load(f)
    for dist in cfg.get("distances", []):
        if dist.get("distance") == distance:
            return dist
    raise ValueError(f"Дистанция '{distance}' не найдена в {config_path}")


def _get_or_create_participant(cursor, event_id: int, p: dict, field_map: dict) -> int:
    start_number = p.get(field_map.get("start_number", "bib"))
    if start_number is None:
        return None
    cursor.execute(
        "SELECT id FROM participants WHERE event_id=%s AND start_number=%s",
        (event_id, start_number),
    )
    row = cursor.fetchone()
    if row:
        return row[0]
    cursor.execute(
        """INSERT INTO participants
           (event_id, start_number, surname, name, birthdate, gender, status, category)
           VALUES (%s,%s,%s,%s,%s,%s,%s,%s)""",
        (
            event_id,
            start_number,
            p.get(field_map.get("surname", "surname"), ""),
            p.get(field_map.get("name", "name"), ""),
            p.get(field_map.get("birthdate", "birthdate")),
            p.get(field_map.get("gender", "gender"), ""),
            p.get(field_map.get("status", "status"), ""),
            p.get(field_map.get("category", "category"), ""),
        ),
    )
    return cursor.lastrowid


def _process_laps(cursor, participant_id: int, event_id: int, runner: dict, lap_count: int, pattern: str):
    inserted = 0
    prev_ms = 0
    for n in range(1, lap_count + 1):
        field = pattern.replace("{n}", str(n))
        cumulative_ms = runner.get(field)
        if cumulative_ms is None:
            break
        lap_ms = cumulative_ms - prev_ms
        cursor.execute(
            """INSERT IGNORE INTO laps (participant_id, event_id, lap_number, cumulative_ms, lap_ms)
               VALUES (%s,%s,%s,%s,%s)""",
            (participant_id, event_id, n, cumulative_ms, lap_ms),
        )
        if cursor.rowcount > 0:
            inserted += 1
        prev_ms = cumulative_ms
    return inserted


def run(config_path: str, interval: int):
    dist_cfg = _load_config(config_path, "24h")
    event_id = dist_cfg["db_event_id"]
    cop = dist_cfg["copernico"]
    race_id = cop["race_id"]
    login = cop["login"]
    preset = cop["preset"]
    event = cop["event"]

    with open(f"config/copernico/{preset}.yaml", encoding="utf-8") as f:
        preset_cfg = yaml.safe_load(f)
    field_map = preset_cfg.get("fields", {})
    lap_fields = preset_cfg.get("lap_fields", {})
    lap_count = lap_fields.get("count", 150)
    lap_pattern = lap_fields.get("pattern", "times.official_{n}kr")

    logger.info(f"▶ Старт загрузчика: event_id={event_id}, interval={interval}s")

    while True:
        try:
            runners = _fetch_copernico(race_id, login, preset, event)
            logger.info(f"✅ Получено {len(runners)} участников")

            conn = _connect()
            cursor = conn.cursor()
            total_inserted = 0
            for runner in runners:
                pid = _get_or_create_participant(cursor, event_id, runner, field_map)
                if pid is None:
                    continue
                total_inserted += _process_laps(cursor, pid, event_id, runner, lap_count, lap_pattern)
            conn.commit()
            cursor.close()
            conn.close()
            if total_inserted:
                logger.info(f"💾 Добавлено новых кругов: {total_inserted}")
            else:
                logger.debug("⏸ Новых кругов нет")
        except Exception as e:
            logger.error(f"❌ Ошибка цикла: {e}")
        time.sleep(interval)


if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--config", required=True)
    parser.add_argument("--interval", type=int, default=30)
    args = parser.parse_args()
    run(args.config, args.interval)
```

- [ ] **Шаг 2: Создать shell-обёртку**

```bash
# deploy/run_tri_loader.sh
#!/bin/bash
set -a
source /opt/km_track/config/loader/"$1".env
set +a
exec /opt/km_track/venv/bin/python load_tri_results.py \
    --config "$LOADER_CONFIG" \
    --interval "${LOADER_INTERVAL:-30}"
```

```bash
chmod +x deploy/run_tri_loader.sh
```

- [ ] **Шаг 3: Создать systemd service template**

```ini
# deploy/km_tri_loader@.service
[Unit]
Description=KM Tri Loader (%i)
After=network.target km_track.service

[Service]
Type=simple
User=km
Group=km
WorkingDirectory=/opt/km_track
EnvironmentFile=/opt/km_track/.env
ExecStart=/opt/km_track/deploy/run_tri_loader.sh %i
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal
SyslogIdentifier=km_tri_loader_%i

[Install]
WantedBy=multi-user.target
```

- [ ] **Шаг 4: Протестировать загрузчик локально** (с SSH-туннелем активным)

```bash
python load_tri_results.py --config config/events/tri_24h.yaml --interval 30
# Ожидать: "✅ Получено N участников"
# Если 0 — уточнить значение поля event в Copernico UI
```

- [ ] **Шаг 5: Закоммитить**

```bash
git add load_tri_results.py deploy/run_tri_loader.sh deploy/km_tri_loader@.service
git commit -m "feat: triatleta loader — fetch laps from Copernico every 30s"
```

---

## Task 4: DB-слой и сервис для FastAPI

**Files:**
- Create: `src/triatleta/__init__.py`
- Create: `src/triatleta/db.py`
- Create: `src/triatleta/service.py`
- Create: `tests/unit/test_tri_service.py`

- [ ] **Шаг 1: Написать падающий тест**

```python
# tests/unit/test_tri_service.py
from src.triatleta.service import calculate_gap


def test_gap_leader_returns_dash():
    assert calculate_gap(10, 3600000, 10, 3600000) == "—"


def test_gap_same_laps_returns_time():
    # отстаёт на 1 минуту (60000 мс)
    assert calculate_gap(10, 3660000, 10, 3600000) == "+00:01:00"


def test_gap_fewer_laps_returns_laps():
    assert calculate_gap(8, 0, 10, 0) == "−2 круга"


def test_gap_one_lap_behind():
    assert calculate_gap(9, 0, 10, 0) == "−1 круг"
```

- [ ] **Шаг 2: Запустить тест — убедиться что падает**

```bash
conda run -n base python -m pytest tests/unit/test_tri_service.py -v
# Ожидать: FAILED — ImportError
```

- [ ] **Шаг 3: Создать пакет и db.py**

```python
# src/triatleta/__init__.py
```

```python
# src/triatleta/db.py
import os
import mysql.connector
from typing import Optional


def get_tri_connection() -> Optional[mysql.connector.MySQLConnection]:
    try:
        return mysql.connector.connect(
            host=os.getenv("DB_HOST", "127.0.0.1"),
            port=int(os.getenv("DB_PORT", "3306")),
            database="triatleta_24h",
            user=os.getenv("DB_USER", "km_analytic"),
            password=os.getenv("DB_PASSWORD"),
            charset="utf8mb4",
            autocommit=True,
            connection_timeout=10,
        )
    except Exception as e:
        import logging
        logging.getLogger(__name__).error(f"❌ triatleta_24h connect error: {e}")
        return None
```

- [ ] **Шаг 4: Создать service.py с расчётом standings**

```python
# src/triatleta/service.py
from typing import Optional
from src.triatleta.db import get_tri_connection


def calculate_gap(
    participant_laps: int,
    participant_elapsed_ms: int,
    leader_laps: int,
    leader_elapsed_ms: int,
) -> str:
    """Отставание от лидера. Вызывается для каждой строки таблицы."""
    if participant_laps == leader_laps and participant_elapsed_ms == leader_elapsed_ms:
        return "—"
    if participant_laps < leader_laps:
        diff = leader_laps - participant_laps
        if diff == 1:
            return "−1 круг"
        elif 2 <= diff <= 4:
            return f"−{diff} круга"
        else:
            return f"−{diff} кругов"
    # одинаковое количество кругов — считаем разницу во времени
    delta_ms = participant_elapsed_ms - leader_elapsed_ms
    total_sec = delta_ms // 1000
    h = total_sec // 3600
    m = (total_sec % 3600) // 60
    s = total_sec % 60
    return f"+{h:02d}:{m:02d}:{s:02d}"


def get_standings(event_id: int, category: Optional[str] = None) -> list[dict]:
    """Возвращает таблицу результатов, отсортированную: больше кругов → меньше времени."""
    conn = get_tri_connection()
    if not conn:
        return []
    try:
        cursor = conn.cursor(dictionary=True)
        cat_filter = "AND p.category = %s" if category else ""
        params = [event_id, category] if category else [event_id]
        cursor.execute(f"""
            SELECT
                p.id,
                p.start_number,
                p.surname,
                p.name,
                p.gender,
                p.category,
                p.team_name,
                COUNT(l.id) AS laps_completed,
                ROUND(COUNT(l.id) * 4.040, 3) AS total_km,
                COALESCE(MAX(l.cumulative_ms), 0) AS elapsed_ms,
                CASE
                    WHEN MAX(l.cumulative_ms) > 0
                    THEN ROUND(COUNT(l.id) * 4.040 / (MAX(l.cumulative_ms) / 3600000.0), 2)
                    ELSE 0.0
                END AS avg_speed_kmh
            FROM participants p
            LEFT JOIN laps l ON l.participant_id = p.id
            WHERE p.event_id = %s {cat_filter}
            GROUP BY p.id
            ORDER BY laps_completed DESC, elapsed_ms ASC
        """, params)
        rows = cursor.fetchall()
        if not rows:
            return []
        leader = rows[0]
        result = []
        for i, row in enumerate(rows):
            row["rank"] = i + 1
            row["gap"] = calculate_gap(
                row["laps_completed"], row["elapsed_ms"],
                leader["laps_completed"], leader["elapsed_ms"],
            )
            row["elapsed_ms"] = int(row["elapsed_ms"])
            result.append(row)
        return result
    finally:
        cursor.close()
        conn.close()


def get_all_laps(event_id: int) -> list[dict]:
    """Все круги события — для сплитов по часам на клиенте."""
    conn = get_tri_connection()
    if not conn:
        return []
    try:
        cursor = conn.cursor(dictionary=True)
        cursor.execute("""
            SELECT l.participant_id, l.lap_number, l.cumulative_ms, l.lap_ms,
                   p.surname, p.name, p.start_number
            FROM laps l
            JOIN participants p ON p.id = l.participant_id
            WHERE l.event_id = %s
            ORDER BY l.participant_id, l.lap_number
        """, (event_id,))
        return cursor.fetchall()
    finally:
        cursor.close()
        conn.close()
```

- [ ] **Шаг 5: Запустить тесты — убедиться что проходят**

```bash
conda run -n base python -m pytest tests/unit/test_tri_service.py -v
# Ожидать: 4 passed
```

- [ ] **Шаг 6: Закоммитить**

```bash
git add src/triatleta/ tests/unit/test_tri_service.py
git commit -m "feat: triatleta service — standings calculation and gap logic"
```

---

## Task 5: FastAPI роуты

**Files:**
- Create: `src/triatleta/router.py`
- Modify: `src/tracker/router.py`

- [ ] **Шаг 1: Создать роутер**

```python
# src/triatleta/router.py
from fastapi import APIRouter, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates
from pathlib import Path

from src.triatleta.service import get_standings, get_all_laps

TRI_EVENT_ID = 1  # id из triatleta_24h.events

router = APIRouter(tags=["Triatleta"])
templates = Jinja2Templates(directory=str(Path(__file__).resolve().parent.parent.parent / "templates"))


def _is_tri_host(request: Request) -> bool:
    host = request.headers.get("host", "")
    return "triatleta" in host or "live-race" in host


@router.get("/tri", response_class=HTMLResponse)
@router.get("/tri/", response_class=HTMLResponse)
async def tri_home(request: Request):
    return templates.TemplateResponse("tri_results.html", {
        "request": request,
        "event_id": TRI_EVENT_ID,
    })


@router.get("/api/tri/standings")
async def tri_standings(category: str = None):
    rows = get_standings(TRI_EVENT_ID, category or None)
    return {"standings": rows}


@router.get("/api/tri/laps")
async def tri_laps():
    return {"laps": get_all_laps(TRI_EVENT_ID)}
```

- [ ] **Шаг 2: Подключить роутер в `src/tracker/router.py`**

Найти строку `router.include_router(webhook_router)` и добавить после неё:

```python
from src.triatleta.router import router as triatleta_router
router.include_router(triatleta_router)
```

- [ ] **Шаг 3: Проверить локально**

```bash
# Запустить .\dev.ps1, затем:
curl http://localhost:8000/api/tri/standings
# Ожидать: {"standings": []} — пустой список (данных ещё нет)
```

- [ ] **Шаг 4: Закоммитить**

```bash
git add src/triatleta/router.py src/tracker/router.py
git commit -m "feat: triatleta FastAPI routes — /tri, /api/tri/standings, /api/tri/laps"
```

---

## Task 6: HTML-страница live-результатов

**Files:**
- Create: `templates/tri_results.html`
- Create: `static/css/tri_results.css`

- [ ] **Шаг 1: Создать CSS**

```css
/* static/css/tri_results.css */
:root {
    --tri-bg: #0d1117;
    --tri-surface: #161b22;
    --tri-border: #30363d;
    --tri-accent: #f0883e;
    --tri-text: #e6edf3;
    --tri-muted: #8b949e;
    --tri-green: #3fb950;
    --tri-red: #f85149;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
    background: var(--tri-bg);
    color: var(--tri-text);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    min-height: 100vh;
}

.tri-header {
    background: var(--tri-surface);
    border-bottom: 2px solid var(--tri-accent);
    padding: 16px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 8px;
}

.tri-header__title { font-size: 20px; font-weight: 700; }
.tri-header__meta { font-size: 13px; color: var(--tri-muted); }
.tri-elapsed { font-size: 22px; font-weight: 700; color: var(--tri-accent); font-variant-numeric: tabular-nums; }

.tri-container { max-width: 1100px; margin: 0 auto; padding: 24px 16px; }

.tri-tabs { display: flex; gap: 4px; margin-bottom: 16px; }
.tri-tab {
    padding: 8px 20px;
    border: 1px solid var(--tri-border);
    border-radius: 6px;
    background: none;
    color: var(--tri-muted);
    cursor: pointer;
    font-size: 14px;
    font-weight: 600;
}
.tri-tab.active { background: var(--tri-accent); color: #000; border-color: var(--tri-accent); }

.tri-table-wrap { overflow-x: auto; }

table.tri-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
}

.tri-table th {
    background: var(--tri-surface);
    color: var(--tri-accent);
    font-weight: 600;
    padding: 10px 12px;
    text-align: left;
    border-bottom: 2px solid var(--tri-border);
    white-space: nowrap;
}

.tri-table td {
    padding: 10px 12px;
    border-bottom: 1px solid var(--tri-border);
    color: var(--tri-text);
}

.tri-table tr:hover td { background: rgba(255,255,255,0.03); }
.tri-rank { font-weight: 700; width: 36px; }
.tri-gap--behind { color: var(--tri-red); }
.tri-gap--leader { color: var(--tri-green); font-weight: 700; }
.tri-speed { font-variant-numeric: tabular-nums; }

/* Сплиты */
.tri-splits { margin-top: 32px; }
.tri-splits__title { font-size: 16px; font-weight: 700; margin-bottom: 16px; }
.tri-slider-wrap { display: flex; align-items: center; gap: 16px; margin-bottom: 16px; flex-wrap: wrap; }
.tri-slider-label { font-size: 14px; color: var(--tri-muted); min-width: 160px; }
input[type=range] { accent-color: var(--tri-accent); width: 160px; }

.tri-refresh { font-size: 12px; color: var(--tri-muted); margin-top: 8px; }
```

- [ ] **Шаг 2: Создать HTML-шаблон**

```html
<!-- templates/tri_results.html -->
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Triatleta — Суточная велогонка 24ч</title>
    <link rel="stylesheet" href="/static/css/tri_results.css?v={{ v }}">
</head>
<body>
    <div class="tri-header">
        <div>
            <div class="tri-header__title">🚴 Triatleta — Суточная велогонка 24ч</div>
            <div class="tri-header__meta">Старт: 25.06.2026 20:00 Красноярск &nbsp;|&nbsp; Круг: 4.04 км</div>
        </div>
        <div>
            <div class="tri-header__meta">Прошло</div>
            <div class="tri-elapsed" id="elapsed">—</div>
        </div>
    </div>

    <div class="tri-container">
        <!-- Вкладки -->
        <div class="tri-tabs">
            <button class="tri-tab active" onclick="switchTab('individual', this)">Личный зачёт</button>
            <button class="tri-tab" onclick="switchTab('relay', this)">Командный зачёт</button>
        </div>

        <!-- Таблица -->
        <div class="tri-table-wrap">
            <table class="tri-table" id="standings-table">
                <thead>
                    <tr>
                        <th class="tri-rank">#</th>
                        <th>Участник</th>
                        <th>Кругов</th>
                        <th>Км</th>
                        <th>Время</th>
                        <th>Скорость</th>
                        <th>Отставание</th>
                    </tr>
                </thead>
                <tbody id="standings-body">
                    <tr><td colspan="7" style="text-align:center;color:var(--tri-muted);padding:24px">Загрузка...</td></tr>
                </tbody>
            </table>
        </div>
        <div class="tri-refresh" id="refresh-label"></div>

        <!-- Сплиты по часам -->
        <div class="tri-splits">
            <div class="tri-splits__title">Сплиты по часам</div>
            <div class="tri-slider-wrap">
                <label class="tri-slider-label">
                    С часа: <span id="from-val">1</span>
                    <input type="range" id="from-slider" min="1" max="24" value="1" oninput="onSlider()">
                </label>
                <label class="tri-slider-label">
                    По час: <span id="to-val">24</span>
                    <input type="range" id="to-slider" min="1" max="24" value="24" oninput="onSlider()">
                </label>
            </div>
            <div class="tri-table-wrap">
                <table class="tri-table" id="splits-table">
                    <thead>
                        <tr>
                            <th>Участник</th>
                            <th>Кругов за период</th>
                            <th>Км за период</th>
                        </tr>
                    </thead>
                    <tbody id="splits-body">
                        <tr><td colspan="3" style="text-align:center;color:var(--tri-muted);padding:16px">Ожидание данных...</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script>
    const GUN_UTC_MS = new Date('2026-06-25T13:00:00Z').getTime();
    const LAP_KM = 4.040;
    let allLaps = [];
    let currentCategory = 'individual';
    let lastUpdated = null;

    // ---- Счётчик прошедшего времени ----
    function updateElapsed() {
        const now = Date.now();
        const diff = Math.max(0, now - GUN_UTC_MS);
        const h = Math.floor(diff / 3600000);
        const m = Math.floor((diff % 3600000) / 60000);
        const s = Math.floor((diff % 60000) / 1000);
        document.getElementById('elapsed').textContent =
            `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;
    }
    setInterval(updateElapsed, 1000);
    updateElapsed();

    // ---- Форматирование мс → чч:мм:сс ----
    function fmtMs(ms) {
        if (!ms) return '—';
        const s = Math.floor(ms / 1000);
        const h = Math.floor(s / 3600);
        const m = Math.floor((s % 3600) / 60);
        const ss = s % 60;
        return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(ss).padStart(2,'0')}`;
    }

    // ---- Загрузка standings ----
    async function loadStandings() {
        try {
            const url = `/api/tri/standings?category=${currentCategory}`;
            const r = await fetch(url);
            const data = await r.json();
            renderStandings(data.standings || []);
            lastUpdated = new Date();
            document.getElementById('refresh-label').textContent =
                `Обновлено: ${lastUpdated.toLocaleTimeString('ru-RU')}`;
        } catch(e) {
            console.error('standings error', e);
        }
    }

    function renderStandings(rows) {
        const tbody = document.getElementById('standings-body');
        if (!rows.length) {
            tbody.innerHTML = '<tr><td colspan="7" style="text-align:center;color:var(--tri-muted);padding:24px">Нет данных</td></tr>';
            return;
        }
        tbody.innerHTML = rows.map(r => {
            const gapClass = r.gap === '—' ? 'tri-gap--leader' : 'tri-gap--behind';
            return `<tr>
                <td class="tri-rank">${r.rank}</td>
                <td>${r.surname} ${r.name}</td>
                <td>${r.laps_completed}</td>
                <td>${parseFloat(r.total_km).toFixed(1)}</td>
                <td style="font-variant-numeric:tabular-nums">${fmtMs(r.elapsed_ms)}</td>
                <td class="tri-speed">${parseFloat(r.avg_speed_kmh).toFixed(1)} км/ч</td>
                <td class="${gapClass}">${r.gap}</td>
            </tr>`;
        }).join('');
    }

    // ---- Загрузка кругов для сплитов ----
    async function loadLaps() {
        try {
            const r = await fetch('/api/tri/laps');
            const data = await r.json();
            allLaps = data.laps || [];
            renderSplits();
        } catch(e) {
            console.error('laps error', e);
        }
    }

    // ---- Сплиты по часам ----
    function onSlider() {
        const fromH = parseInt(document.getElementById('from-slider').value);
        let toH = parseInt(document.getElementById('to-slider').value);
        if (toH < fromH) { toH = fromH; document.getElementById('to-slider').value = fromH; }
        document.getElementById('from-val').textContent = fromH;
        document.getElementById('to-val').textContent = toH;
        renderSplits();
    }

    function renderSplits() {
        const fromH = parseInt(document.getElementById('from-slider').value);
        const toH = parseInt(document.getElementById('to-slider').value);
        const fromMs = (fromH - 1) * 3600000;
        const toMs = toH * 3600000;

        // Группируем по участнику
        const byParticipant = {};
        for (const lap of allLaps) {
            if (lap.cumulative_ms > fromMs && lap.cumulative_ms <= toMs) {
                const key = lap.participant_id;
                if (!byParticipant[key]) {
                    byParticipant[key] = { name: `${lap.surname} ${lap.name}`, count: 0 };
                }
                byParticipant[key].count++;
            }
        }

        const rows = Object.values(byParticipant).sort((a,b) => b.count - a.count);
        const tbody = document.getElementById('splits-body');
        if (!rows.length) {
            tbody.innerHTML = '<tr><td colspan="3" style="text-align:center;color:var(--tri-muted);padding:16px">Нет кругов в выбранном диапазоне</td></tr>';
            return;
        }
        tbody.innerHTML = rows.map(r => `<tr>
            <td>${r.name}</td>
            <td>${r.count}</td>
            <td>${(r.count * LAP_KM).toFixed(1)}</td>
        </tr>`).join('');
    }

    // ---- Переключение вкладок ----
    function switchTab(category, btn) {
        currentCategory = category;
        document.querySelectorAll('.tri-tab').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        loadStandings();
    }

    // ---- Polling ----
    loadStandings();
    loadLaps();
    setInterval(() => { loadStandings(); loadLaps(); }, 30000);
    </script>
</body>
</html>
```

- [ ] **Шаг 3: Проверить локально**

```bash
# Запустить .\dev.ps1
# Открыть http://localhost:8000/tri
# Ожидать: страница открывается, таблица показывает "Нет данных", счётчик тикает
```

- [ ] **Шаг 4: Закоммитить**

```bash
git add templates/tri_results.html static/css/tri_results.css
git commit -m "feat: triatleta live results page with hourly splits slider"
```

---

## Task 7: nginx + SSL

**Files:**
- Modify: `deploy/nginx.conf`

- [ ] **Шаг 1: Добавить server blocks в `deploy/nginx.conf`**

Добавить в конец http-блока (перед закрывающей `}`):

```nginx
    # ---- live-race.triatleta.ru (placeholder до получения SSL) ----
    server {
        listen 80;
        server_name live-race.triatleta.ru;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name live-race.triatleta.ru;

        ssl_certificate     /etc/letsencrypt/live/live-race.triatleta.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/live-race.triatleta.ru/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        location /static/ {
            alias /opt/km_track/static/;
            expires 7d;
            add_header Cache-Control "public, max-age=604800";
        }

        location / {
            proxy_pass          http://127.0.0.1:8000;
            proxy_set_header    Host $host;
            proxy_set_header    X-Real-IP $remote_addr;
            proxy_set_header    X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header    X-Forwarded-Proto $scheme;
            proxy_read_timeout  60s;
        }
    }
```

- [ ] **Шаг 2: Проверить конфиг локально**

```bash
nginx -t  # если nginx установлен локально
# или просто убедиться что нет синтаксических ошибок визуально
```

- [ ] **Шаг 3: Закоммитить nginx конфиг**

```bash
git add deploy/nginx.conf
git commit -m "feat: nginx server block for live-race.triatleta.ru"
git push origin main
```

- [ ] **Шаг 4: Проверить что DNS распространился**

```bash
# С локальной машины:
ssh root@89.108.88.104 "dig +short live-race.triatleta.ru"
# Ожидать: 89.108.88.104
```

- [ ] **Шаг 5: Получить SSL сертификат (только после шага 4)**

```bash
ssh root@89.108.88.104
# Временно добавить HTTP server block для ACME challenge:
certbot certonly --nginx -d live-race.triatleta.ru
# Ожидать: "Successfully received certificate"
# Путь: /etc/letsencrypt/live/live-race.triatleta.ru/fullchain.pem
```

- [ ] **Шаг 6: Перезагрузить nginx после деплоя**

```bash
ssh root@89.108.88.104 "nginx -t && systemctl reload nginx"
# Ожидать: "nginx: configuration file test is successful"
```

---

## Task 8: Деплой загрузчика на VPS

**Files:** только команды на VPS

- [ ] **Шаг 1: Скопировать systemd service и запустить**

```bash
ssh root@89.108.88.104
cp /opt/km_track/deploy/km_tri_loader@.service /etc/systemd/system/km_tri_loader@.service
chmod +x /opt/km_track/deploy/run_tri_loader.sh
systemctl daemon-reload
systemctl enable km_tri_loader@tri_24h
systemctl start km_tri_loader@tri_24h
```

- [ ] **Шаг 2: Проверить что загрузчик запустился**

```bash
systemctl status km_tri_loader@tri_24h
journalctl -u km_tri_loader@tri_24h -n 20
# Ожидать: "✅ Получено N участников" или "❌" с деталью ошибки
```

- [ ] **Шаг 3: Если 0 участников — проверить event в Copernico**

Открыть в браузере:
```
https://public-api.copernico.cloud/api/races/triatleta-sutochnaya-velogonka-24-chasa-2026/preset/podbor250718@gmail.com:::tri_24h_2026/24h
```
Если 404 — уточнить значение поля `event` в Copernico UI и обновить `config/events/tri_24h.yaml`.

---

## Task 9: Smoke-тест перед стартом гонки

- [ ] Открыть `https://live-race.triatleta.ru/tri` — страница загружается, счётчик тикает
- [ ] `GET https://live-race.triatleta.ru/api/tri/standings` — возвращает JSON
- [ ] `GET https://live-race.triatleta.ru/api/tri/laps` — возвращает JSON
- [ ] `systemctl status km_tri_loader@tri_24h` — active (running)
- [ ] После появления первых участников в Copernico: таблица обновляется

---

## Task 10: Тестовые данные и локальная верификация

**Цель:** Проверить весь UI (`/24h`, standings, сплиты) без ожидания реальных данных от Copernico.

**Files:**
- `seed_tri_test_data.py` — уже создан

**Данные:** 14 участников (10 личный зачёт + 4 эстафеты), ~65-100 кругов каждый (имитация ~15ч гонки)

- [ ] **Шаг 1: Открыть SSH-туннель на локальную машину**

```powershell
# В отдельном терминале (Windows):
ssh -L 3306:127.0.0.1:3306 root@89.108.88.104 -N
```

- [ ] **Шаг 2: Убедиться что БД и таблицы созданы (Task 1)**

```bash
ssh root@89.108.88.104 "mysql -u km_analytic -pkm_password triatleta_24h -e 'SHOW TABLES'"
# Ожидать: events, participants, laps
```

- [ ] **Шаг 3: Запустить seed-скрипт**

```powershell
conda run -n base python seed_tri_test_data.py
# Ожидать: "Готово. Вставлено новых кругов: ~7500"
```

- [ ] **Шаг 4: Перезапустить seed с нуля (если нужно)**

```powershell
conda run -n base python seed_tri_test_data.py --reset
```

- [ ] **Шаг 5: Запустить приложение и проверить страницу**

```powershell
.\dev.ps1
# Открыть http://localhost:8000/24h
```

  Проверить:
  - Таблица standings показывает 14 участников
  - Колонки: #, Участник, Зачёт, Кругов, Км, Время, Скорость, Отставание
  - Лидер: бейдж «Личный», gap = «—»
  - Второй и далее: красное отставание
  - Сплиты по часам работают (слайдер)
  - Фильтр по полу работает
  - Поиск по фамилии работает

- [ ] **Шаг 6: Проверить API напрямую**

```bash
curl http://localhost:8000/api/tri/standings | python -m json.tool | head -40
curl http://localhost:8000/api/tri/laps | python -m json.tool | head -20
```

---

## Открытые вопросы (решить до Task 3)

1. ~~**Точное название `event` в Copernico**~~ — ✅ подтверждено: `"24h race"`
2. **Эстафета** — как Copernico представляет команды. Командный зачёт (`switchTab('relay')`) пока показывает пустую таблицу — реализовать после уточнения структуры.
