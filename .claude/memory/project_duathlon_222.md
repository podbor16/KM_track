---
name: project-duathlon-222
description: Дуатлон 222 — новая гонка 05.09.2026 (проект Triatleta), дизайн и план готовы, ждём Copernico от организаторов
metadata:
  type: project
---

Новая гонка **Дуатлон 222** (05.09.2026, один массовый старт, 3 этапа: бег 10 км → вело 170 км → бег 42 км, зачёт только абсолютный М/Ж, без возрастных категорий, без эстафет). Проект того же организатора «Triatleta», что и суточная велогонка ([[project_km_track]] — там же прецедент `src/triatleta/`).

**Дизайн и план зафиксированы 2026-08-24** после сессии вопрос-ответ с пользователем:
- Дизайн-док: `docs/superpowers/specs/2026-08-24-duathlon-222-design.md`
- План с чекбоксами: `docs/superpowers/plans/2026-08-24-duathlon-222.md`

**Ключевые решения:**
- Трекер/GPX-карта не нужны — гонка круговая (как суточная велогонка), точки на маршруте бессмысленны.
- Хостинг: тот же домен `live-race.triatleta.ru`, путь `/duathlon222` (не отдельный поддомен) — переиспользует уже выпущенный SSL.
- Формат старта: один общий массовый старт, дальше личное непрерывное время (T1/T2 без гандикапа) — как в классическом дуатлоне.
- На странице результатов нужны явные колонки по этапам (Бег-1/Вело/Бег-2), не только общее время.

**Архитектурный выбор (важно для будущих сессий):** в проекте два прецедента многосоставных гонок — `src/triatleta/` (tri_24h, N одинаковых кругов, таблица `laps`) и `src/siberman/` (разные дисциплины, широкие колонки по этапам на участника). Дуатлон 222 структурно ближе к Siberman (3 РАЗНЫЕ дисциплины, нужны отдельные колонки по этапам) — поэтому выбрана модель широких колонок (`run1_s`/`bike_s`/`run2_s` в таблице `participants` новой БД `duathlon_222`, время этапа = вычитание соседних кумулятивных отметок), а не модель кругов tri_24h. Данные при этом берутся live-поллингом из Copernico API как у tri_24h (не Excel-парсинг как в Siberman) — гибрид двух прецедентов.

**Блокер (Task 0/1/3 в плане):** структура Copernico и точное время старта (`gun_time`) 05.09.2026 всё ещё не подтверждены организаторами (последняя проверка 2026-09-02, за 3 дня до гонки). Task 0: рабочее предположение — один event с полями трёх этапов (Вариант А), конфиги написаны под него с плейсхолдерными именами полей (`times.official_run1`/`bike`/`finish` — догадка по конвенции, не подтверждено). Task 1 частично готово: БД `duathlon_222` создана и применена на VPS (2026-09-02, `src/duathlon222/migrations/001_init.sql` через `python -m deploy.ssh_apply_duathlon222_migration`), но запись в `events` ещё не вставлена — ждёт `gun_time`. Task 3 (загрузчик `load_duathlon_results.py`) написан, но не запускался вживую — ждёт реальных полей Copernico.

**2026-09-02: реализовано всё, что НЕ зависит от Copernico/gun_time** (Tasks 4-7 плана) — DB-слой (`src/duathlon222/db.py`/`service.py`), полный FastAPI-модуль с админкой (`src/duathlon222/router.py`: логин/сессия, управление systemd-загрузчиком, редактор YAML-пресета в браузере — по образцу `race_triatleta`), HTML-страницы (простая таблица с колонками по этапам, без графиков — сознательно проще боевого `tri_results.html`), правка `deploy/nginx.conf` (домен `live-race.triatleta.ru` держит explicit whitelist путей, не catch-all — правка была обязательна). Всё локально протестировано через `TestClient`, полный набор `tests/unit/` 408/408 без регрессий. **Важная находка по ходу:** реальная кодовая база продукта называется `src/race_triatleta/` (не `src/triatleta/` — переименовано в `docs/superpowers/plans/2026-07-06-project-restructure.md`), заметно богаче изначального MVP-дизайна суточной гонки — при работе с этим продуктом в будущем сверяться с актуальным кодом, не со старыми design-doc'ами.

**2026-09-03: URL стали year-scoped** — `/duathlon222` → `/duathlon222_2026` (307-редирект со старого URL для совместимости), `/api/duathlon222_2026/standings`, `/duathlon222_2026/participant/{bib}`. Реализовано через существующую инфраструктуру `src/config/event_loader.py::load_events_cached()` — год резолвится в `db_event_id` по полю `code: "duathlon_222_{year}"` в `config/events/*.yaml`; новый год = новый YAML-файл с этим code, без правок роутера. Admin/login/логика загрузчика остались НЕ year-scoped (`/duathlon222/admin` и т.п.) — это управление "текущей" гонкой, не архив.

**Найден и исправлен реальный баг по ходу:** `config/events/duathlon_222.yaml` не проходил валидацию `EventConfig`/`DistanceConfig` (не хватало обязательных полей `display_name` на верхнем уровне и `distance_km` у дистанции) — `load_all_events()` тихо пропускала файл с warning в лог (try/except per-file), поэтому `load_events_cached()` никогда не находил duathlon_222 и любой год отдавал 404. **Урок:** свой собственный YAML-конфиг для нового события нужно с самого начала валидировать против реальных pydantic-моделей в `src/config/event_loader.py` (`EventConfig`/`DistanceConfig`), а не только против неформальной структуры из design-doc — расхождение осталось незамеченным, пока не появился код, реально идущий через `load_events_cached()` (раньше `load_duathlon_results.py`/`router.py` читали тот же YAML напрямую через `yaml.safe_load()`, в обход pydantic-валидации, и никогда не спотыкались об это). Попутно обнаружено (не исправлено — вне задачи): `config/events/tri_24h.yaml` страдает той же проблемой валидации, существующий код для tri_24h тоже никогда не проходит через `EventConfig`.

**2026-09-02 (продолжение, тот же день):** запушен коммит с полным Task 4-7 + карточка на лендинге + `location = /` в nginx (был обязателен — прод-нюанс: у этого домена явный whitelist путей, а не catch-all). Деплой прошёл, прод-эндпоинты проверены curl'ом.

Следом пользователь отметил, что страница результатов выглядит «сыровато» — не хватало последней пройденной отметки и прогноза финиша этапа. Уточнил: организаторы подтвердили параметры кругов (гонка круговая на всех 3 этапах) — **Бег-1: 4 круга по ~2.5 км, Вело: 42 круга по ~4.04 км, Бег-2: 12 кругов по ~3.5 км** (4×2.5=10, 42×4.04≈170, 12×3.5=42 — сходится с дистанциями этапов). Добавлена таблица `checkpoints` (миграция `002_checkpoints.sql`, применена на проде) + логика прогноза `_forecast_stage_finish()` в `service.py` (экстраполяция по средней скорости пройденных кругов текущего этапа, тот же принцип что `forecastTime()` в Siberman, но на Python с unit-тестами вместо JS). На странице результатов теперь: бейдж текущего этапа, тикающие часы «сколько уже на этом этапе», круг N/M, прогноз финиша этапа (часы), средняя скорость по завершённым этапам. Загрузчик (`load_duathlon_results.py`) и Copernico-пресет (`stage_lap_fields`) обновлены под круги — имена полей Copernico всё ещё догадка, не подтверждены. Этот раунд закоммичен и задеплоен (коммит `44654e5`).

**2026-09-02 (третий раунд, тот же день) — карточка участника.** Пользователь попросил карточку участника «как на Siberman» с бегунками (прогресс-бар с точкой-маркером) и подробными отметками. Изучен реальный `templates/siberman/participant.html` (progressBarHtml/stageProgressBarHtml — сегментированный трек с `.pb-marker-dot`, аккордеон `<details>` по этапам с таблицей КТ). Реализован упрощённый аналог для Дуатлона 222 (без эстафет/мультидневного гандикапа/records — их тут нет): `get_participant()` в `service.py` (переиспользует `get_standings()` для общего места, не дублирует сортировку), два новых чистых хелпера `_lap_ranks_from_rows()`/`_build_stage_laps()` (полностью unit-тестируемые без БД — 5 новых тестов, 424/424 всего), роуты `/duathlon222/participant/{start_number}` + `/api/duathlon222/participant/{start_number}`, шаблон `duathlon_participant.html` (общий бегунок по всей гонке 0-222км + свой бегунок на каждом этапе в аккордеоне + таблица кругов со сплитом/скоростью/местом-пол/местом-абсолют). Строки в `duathlon_results.html` теперь кликабельны — ведут на карточку.

**Верификация реальными данными:** временно вставлен тестовый участник+круги в прод-БД (event_id=1, start_number=999) через plink, само событие `events.id=1` создавалось этим тестом впервые (таблица была пустая) — после проверки удалено и `AUTO_INCREMENT` явно сброшен на 1 (`ALTER TABLE ... AUTO_INCREMENT=1`), чтобы будущая реальная запись события не получила `id=2` и не разошлась с захардкоженным `DUATHLON_EVENT_ID=1` в `router.py`. `get_participant()` вызван напрямую через временный SSH-туннель (не полный деплой) — прогноз финиша/скорости/места совпали с ручным расчётом. **Урок на будущее:** таблицы с пустым auto_increment и хардкоженным ожидаемым id в коде — тестовые вставки в них обязательно сопровождать явным сбросом `AUTO_INCREMENT`, иначе тихо ломается допущение "id=1" при следующей реальной вставке.

Ещё не закоммичено — ждёт решения пользователя.

**Why:** дедлайн 05.09.2026, гонка новая, нужна отдельная от krasmarafon/tri_24h/siberman инфраструктура, чтобы не рисковать регрессией в уже работающих продуктах (тот же принцип изоляции, что у `load_tri_results.py` не трогающего `load_race_results.py`).

**How to apply:** при продолжении работы — сверяться с планом `docs/superpowers/plans/2026-08-24-duathlon-222.md`, не перепрыгивать Task 0. Логин Copernico предположительно тот же аккаунт, что у tri_24h (`podbor250718@gmail.com`) — это ДОПУЩЕНИЕ дизайн-дока, не подтверждённый факт, проверить при первом реальном контакте с Copernico для этой гонки.

**2026-09-03 (вечер): race_id и event подтверждены пользователем** — `race_id: "duatlon-222-2026"`, `event: "222"` (обновлено в `config/events/duathlon_222.yaml`). Живой fetch подтвердил: race_id/login/event распознаются Copernico (`{"error":"No preset"}`, не 404/ошибка гонки) — но на тот момент **пресет ещё не был создан в Copernico UI**.

**2026-09-03 (позже тем же вечером) — Task 0/1 практически закрыты:**
- Пользователь создал пресет `222_2026` в Copernico UI. Живой fetch (`.../races/duatlon-222-2026/preset/podbor250718@gmail.com:::222_2026/222`) вернул 15 реальных участников — **поля участника подтверждены**: `dorsal`/`surname`/`name`/`gender`/`status` совпадают с уже написанным `fields:`-маппингом в `config/copernico/duathlon_222_2026.yaml` 1:1, правок не потребовалось.
- **Поля тайминга (`stage_fields`/`stage_lap_fields`) всё ещё ДОГАДКА** — в живом ответе НЕТ НИ ОДНОГО ключа `times.*`, хотя пользователь подтвердил, что добавил поля отсечек в пресет. У всех участников `status="notstarted"` — похоже, Copernico не сериализует пустые/null-поля вовсе (sparse JSON), значит узнать реальные имена полей тайминга можно только по первой настоящей отметке. Пользователь подтвердил, что нет доступа вручную вписать тестовую отметку в Copernico — придётся ждать реального старта гонки (05.09.2026) и проверять имена полей "на лету" (админка уже позволяет править YAML-пресет в браузере без деплоя — специально для этого сценария).
- **gun_time подтверждён**: судья ввёл в Copernico "06:59:59.998" (дата в самом Copernico — 2025-09-27, подтверждено пользователем как остаток шаблона прошлого года, реальная дата гонки — 05.09.2026, как и договаривались всю сессию). Интерпретировано как ~07:00 Красноярск (UTC+7) = `2026-09-05 00:00:00 UTC`.
- **Task 1 закрыт**: `INSERT INTO duathlon_222.events (code, name, gun_datetime) VALUES ('duathlon_222_2026', 'Дуатлон 222', '2026-09-05 00:00:00')` выполнен на проде (id=1, ровно как ожидал захардкоженный когда-то `DUATHLON_EVENT_ID=1` — впрочем, эта переменная уже убрана из router.py в пользу year-scoped резолвинга). `config/events/duathlon_222.yaml`: `db_event_id: 1`, `gun_time: "00:00:00"`. Фронтенд-плейсхолдеры `GUN_UTC_MS` в `duathlon_results.html`/`duathlon_participant.html` обновлены на реальное значение.

**Остаётся единственный настоящий блокер:** реальные имена Copernico-полей тайминга — узнать можно только по факту первой отметки в день гонки (05.09.2026). Всё остальное готово: БД, конфиги, загрузчик (безопасно деградирует на неизвестных полях тайминга — не падает, просто ничего не пишет), админка для правки пресета вживую.
