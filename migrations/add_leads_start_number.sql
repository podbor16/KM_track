-- Стартовый номер (bib), присвоенный организатором ДО гонки — часть
-- "обработанного" стартового списка (Excel-файл с уже расставленными
-- номерами, см. tilda_import_parser.py — колонка "Номер"). Не путать с
-- results.start_number (тот появляется только ПОСЛЕ гонки, из Copernico) —
-- это независимое, более раннее presence поля, может отсутствовать у
-- большинства заявок.
-- 2026-08-18

ALTER TABLE leads ADD COLUMN start_number INT UNSIGNED NULL;
