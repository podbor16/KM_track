-- Источник создания заявки — webhook (оплата через Tilda) или import
-- (bulk-импорт CSV/Excel из /admin, см. bulk_import_leads()). Нужен, чтобы
-- get_age_group_label() мог по-разному обрабатывать регистрации младше
-- минимального возраста дистанции: для импорта (обработанный организатором
-- файл) — присваивать минимальную категорию, для вебхука — по-прежнему
-- "Ошибка возраста" (сигнал реальной проблемы данных, требует ручной
-- проверки организатором). DEFAULT 'webhook' — не переопределяем задним
-- числом источник уже существующих заявок (подавляющее большинство
-- пришло через вебхук).
-- 2026-08-19

ALTER TABLE leads ADD COLUMN source ENUM('webhook', 'import') NOT NULL DEFAULT 'webhook';
