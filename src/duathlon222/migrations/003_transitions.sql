USE duathlon_222;

-- Транзитные зоны (Т1: бег-1→вело, Т2: вело→бег-2) — учитываются в общем
-- времени гонки (уже входят внутрь run2_s, ничего пересчитывать не нужно),
-- но должны ВЫЧИТАТЬСЯ из "чистого" времени соседних этапов и показываться
-- отдельной колонкой. Без этих полей время этапа (bike_s/run2_s = разница
-- соседних отметок) молча включало транзит внутрь этапа.
ALTER TABLE participants ADD COLUMN t1_s INT DEFAULT NULL;
ALTER TABLE participants ADD COLUMN t2_s INT DEFAULT NULL;
