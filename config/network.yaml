# Настройки сети (машинозависимые, не менять при смене года)
# Используется с флагом --fix-routing для обхода VPN при подключении к БД
#
# Как узнать локальный шлюз:
#   ipconfig -> "Основной шлюз" для вашего Wi-Fi/Ethernet адаптера
local_gateway: "192.168.0.1"   # Wi-Fi шлюз (Беспроводная сеть)
