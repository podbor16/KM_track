# Задача: Мелкие фиксы и фичи утром 26.06

**Статус:** В очереди  
**Приоритет:** Высокий (гонка идёт)

## Чеклист

- [x] **1. Ось X на графиках — время от старта, не время суток**
  - Скорость и Позиция: метки `1ч`, `2ч`, ..., `24ч` вместо `20:00`, `21:00`, ...
  - Tooltip title тоже в формате «0.0–0.5 ч от старта»

- [x] **2. Сплиты: ограничить доступный диапазон пройденными часами**
  - Функции `currentRaceH()` и `updateSliderMax()` добавлены
  - Вызываются после `loadStandings()` и `loadLaps()`

- [x] **3. Лучший круг за всю гонку**
  - Баннер `#best-lap-banner` под рекордом 2025
  - Обновляется в `renderStandings()` из `bestLapLeader`

- [x] **4. Индикация схода участника**
  - `p.status` добавлен в SELECT в `service.py`
  - CSS `.tri-dnf-badge` добавлен в `tri_results.css`
  - Бейдж «Сошёл» при status in ['withdrawn','abandoned','dnf']
  - ⚠️ Загрузчик: фильтр для DNF в лоадере — не реализован (нет лоадера в repo)

## Заметки
- Проверить поле `status` в Copernico API для схода: `runner.get('status')`
- Ось X: в tooltip тоже переделать формат на «Ч от старта»
