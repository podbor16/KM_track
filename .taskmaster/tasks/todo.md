# Результаты прошлых лет (карта — Obsidian `karta-propuskov-rezultatov`)

- [x] Х Трейл 2025, 10 км (event 95): 758 строк, финиш 649 — трекер Икса 2026: личный темп 30,8%, категория 67,4%, 6:00 — 1,8%
- [x] Снежная семёрка 2025, 7 км (event 99): 598 строк, финиш 504 — гандикап (волны 11:00–11:15), места по Finish, чистые места по «чистое»
- [ ] К Снежной 2026 (06.12): категории 2025 — только «Женщины»/«Мужчины»; сверить с категориями 2026 в Copernico и при необходимости дополнить `_CAT_MAP`; проверить трекер с гандикапом (задержка волны в time_gun_start)
- [ ] Остальные 2025 (Жара 5/10/21.1, Женская семёрка, Детский) и 2024 и раньше — по файлам пользователя: `scripts/import_results_xlsx.py --event-id <id> --xlsx …`

---

# Бэклог: исчерпание пула соединений в одном воркере (26.09 16:02–18:52)

- Воркер pid 1420737: все 10 соединений пула заняты ~3 ч без нагрузки, две фоновые задачи каждые 100 мс получали «pool exhausted» (~118 тыс. строк в журнале); остальные воркеры в норме; к 18:52 освободилось само
- Статическая проверка: все get_pooled_connection закрываются в finally; SSE соединений не держит. Гипотезы: вложенное получение соединения внутри фоновой задачи (взаимная блокировка при 10 одновременных), зависшие запросы без таймаута чтения
- Временно: перезапуск km_track перед стартом и при росте ошибок (runbook). Разобрать после 27.09: backoff в фоновых задачах вместо повтора каждые 100 мс, таймауты соединения, метрика занятых соединений пула

---

# Задача: аудит карточек клиентов (ФИО, дубли)

**Статус:** ⏳ В РАБОТЕ (2026-09-26) — выгрузка на проверку пользователю

- [x] Аудит на чтение (23 118 карточек): поля ФИО (пробелы, латиница, регистр, отчества, перестановки, ФИО в обоих полях) и 10 категорий возможных дублей D1–D8
- [x] Выгрузка `Downloads/Аудит_клиентов_2026-09-26.xlsx` (сводка + лист на категорию, колонка «Решение»)
- [x] Решения пользователя: склеивать все D1–D8; чинить пробелы, регистр, отчества, лишние слова, перестановки; латиницу → кириллицу, где это русское ФИ (реальные иностранцы — латиницей), с ручной проверкой было/станет; одна буква — найти карточку по фамилии+ДР+контакт
- [ ] `scripts/clean_clients.py`: канонические ФИО (чистые функции + unit-тесты), транслит латиницы с подбором по частотным именам/фамилиям и полу, группы склейки (union-find, категории D1–D8 + коллизии после чистки + «одна буква»), выбор выжившей карточки (результаты > заявки > настоящая ДР), -а в фамилии по полу
- [x] Dry-run на проде → xlsx было/станет → проверено пользователем (`Downloads/Чистка_клиентов_было_стало.xlsx`: 25 правок латиницы, 17 групп разделены на родителя/ребёнка, красные строки и «Не решено» — исключить)
- [x] Решения -> `/root/backups/clean_decisions_20260926.json` (`--make-decisions`): 1580 групп, 191 переименование, 11 исключено; заявки 10 исключённых удаляются (кроме «Корж Апероль»), результаты остаются; 3 совпадения после правок склеиваются автоматически
- [ ] **Применить в понедельник 2026-09-28** (решение пользователя: к этому времени работа с результатами Забега Икс 27.09 закончена): повторить dry-run, затем `venv/bin/python scripts/clean_clients.py --decisions /root/backups/clean_decisions_20260926.json --apply --backup /root/backups/clean_clients_<дата>.json`; проверить: uk_client, нет сирот, повторный аудит; обновить карту в Obsidian
- [x] «Михалёва Маруся 2010-04-23» (группа 36585) = «Михалёва Мария» (решение пользователя) — в решениях имя группы «Мария», склеится с 32983
- [ ] Apply: бэкап clients/leads/results затронутых → перенос leads/results на выжившую карточку, удаление склеенных, новые ФИО/ДР в clients/leads/results → полный пересчёт агрегатов (count_leads, total_amount, last_lead/result, first/last_lead_date, first_event) → проверка uk_client, повторный аудит
- [ ] Склейка карточек + исправление ФИО в clients/leads/results (dry-run → бэкап → apply)
- [ ] Нормализация ФИО при загрузке (вебхук, импорт в админке, результаты) — согласовано, после старта 27.09
- Collation `utf8mb4_0900_ai_ci`: регистр и ё/е уже не различаются (uk_client), пробелы в конце, й/и и латиница — различаются

---

# Задача: заявки Весны, Красочного и Женской семерки 2026 (Tilda CSV)

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `24f0e2f`, скрипт `scripts/import_tilda_csv_year.py` (год + пары «старт=CSV»)

- Прод: вставлено 3544 (Весна — 1314, Красочный — 1408, Женская семерка — 822), ошибок 0; дубли — самая ранняя заявка (−10)
- Финишёры 2026 с заявкой: Весна 847/1003, Красочный 815/1079, Женская 552/637; остальных нет в выгрузке Tilda вообще (регистрация вне Tilda)

---

# Задача: заявки Снежной семерки 2022–2025

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `ef65dad`, скрипт `scripts/import_snow7_2022_2025.py`

- Прод: вставлено 1745 (2022 — 294, 2023 — 571, 2024 — 853, 2025 — 27), номер записан в 903 существующих заявки 2025, ошибок 0
- «Стартовый 2023» без строки заголовков — заголовки подставляет обёртка; `main()` принимает функцию `wb -> лист`
- «Пачка» 2025 (103, дата была 24.02.2026) — даты соседних строк; исключена продажа сезона 2023 в листе «2022»
- Этим импортом закрыты все пропуски заявок 2022–2024. Бэкап `/root/backups/snow7_*.json`

---

# Задача: заявки Забега Икс (Х Трейл) 2022–2025

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `898618c`, скрипт `scripts/import_xtrail_2022_2025.py`

- Прод: вставлено 1855 (2023 — 782, 2024 — 1039, 2025 — 34), номер записан в 1155 существующих заявок 2025, ошибок 0; 2022 — всё уже было из «Бума»
- Исключены: `test` (2024), 6 заявок Жары 21.1 км в листе «2022»; 24 строки — тот же человек под другими ДР/ФИО
- Исправлен `_TEXT_DIST_RE`: «21.1 км» читалось как «1 км» (+ тест)
- Бэкап `/root/backups/xtrail_*.json`

---

# Задача: заявки Детского забега 2022–2025 + дистанция по возрасту

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `6da2ea5`, скрипты `scripts/import_kids_2022_2025.py`, `scripts/fix_kids_distances.py`

- Правило пользователя: год старта − год рождения ≤ 5 — 500 м, иначе 1 км (для всех лет)
- Прод: вставлено 2220 (2022 — 701, 2023 — 707, 2024 — 797, 2025 — 15), номер записан в 862 заявки 2025, ошибок 0; «пачка» 2025 (107, дата была 24.02.2026) получила даты листа
- Существующие 95 заявок (2013, 2020, 2021) переведены на правильную дистанцию, создано событие 2013 / 500 м
- Код: `distance_fn` в `_record`/`parse_google_sheet`/`parse_start_list`/`main()`, `parse_birthday(min_age=)`, первая колонка при повторе заголовка, номер без заголовка, последний день регистрации без даты «пачки»
- ✅ Даты рождения из файла (`scripts/fix_kids_birthdays.py`, коммит `7f6d1d9`): 49 из 50 заявок с заглушкой 2022–2025; 11 перепривязаны к карточкам с настоящей датой, 38 карточкам поставлена дата (`recover_birthdays.set_birthdays()`), 11 пустых карточек удалены; дистанции — все по правилу; расхождений заявка↔карточка 0. Бэкап `/root/backups/kids_bd_*.json`

---

# Задача: заявки Женской семерки 2022–2025

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `0531d6a`, скрипт `scripts/import_womens7_2022_2025.py` (обёртка над `import_colorrun_2023_2025.main()`)

- Прод: вставлено 1147 (2023 — 500, 2024 — 626, 2025 — 21), номер записан в 789 существующих заявок 2025, ошибок 0; 2022 — всё уже было из «Бума»
- `distance_from_sku`: дистанция из текста продукта, если SKU без года («Girlrun2022», «girl7,»); `main()` принимает событие, листы, окно пачки и `drop` — исключения
- Исключена тестовая продажа на 9,90 ₽ от 17.10.2023 в листе «2023» (решение пользователя); «пачка» 2025 (66) — дата из файла только у 1
- Бэкап `/root/backups/womens7_20260926_115714.json`

---

# Задача: заявки Красочного забега 2023–2025

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `770e20c`, скрипт `scripts/import_colorrun_2023_2025.py`

- Источник: Google xlsx — регистрации «2023», «2024», «LeadsFromTilda» (2025) + «Стартовый 2023/2024/2025» (номера); `parse_start_list()` в общем импортёре
- Прод: вставлено 1804 (2023 — 742, 2024 — 1048, 2025 — 14), номер у 1791; номер записан в 1326 существующих заявок 2025; ошибок 0, у всех событие и клиент
- Участники стартовых листов без регистрации (43) — дата последнего дня регистрации года; для 2025 — максимум по файлу и вебхуку в БД (10.06.2025), а не по LeadsFromTilda (17.01)
- «Пачка» 2025 (291): в LeadsFromTilda дата только у 83 из 396 строк, 290 так и остались 17.01.2025 — источника дат нет
- 24 строки не вставлены (тот же человек в БД под другими ДР/ФИО); удалены 3 клиента-сироты после Весны. Бэкап `/root/backups/colorrun_20260926_114041.json`

---

# Задача: заявки Весны 2023–2025

**Статус:** ✅ ГОТОВО (2026-09-26), коммит `6a39b64`, скрипт `scripts/import_vesna_2023_2025.py`

- Источники: Google xlsx (листы 2023 / 2024 / «Стартовый 2025» — разные форматы) и Tilda CSV 2025
- Решения пользователя: 2023 — всё 5 км; 23 участника стартового списка 2025, которых нет ни в БД, ни в Tilda, — с датой последнего дня регистрации; «пачку» 21.01.2025 (158) не трогаем — источника дат нет; стартовые номера 2023 и 2025 записать
- Прод: вставлено 1811 (2023 — 694, 2024 — 1094, 2025 — 23), номер записан в 1294 существующие заявки 2025, ошибок 0; у всех новых есть событие и клиент; дублей номеров в 2025 нет
- По ходу: SKU Tilda без учёта регистра («Vesna5-2025y»), `insert_leads()` пишет `start_number`

---

# Задача: стартовый список — годы без заявок и неизвестные даты рождения

**Статус:** ✅ ГОТОВО (2026-09-24)

- Селектор года показывал сплошной диапазон 2013..следующий год. Теперь — только годы с заявками по событию (`/api/registered-runners-years` c `years_from_leads_only`; годы, где событие только заведено в `events`, для стартового списка не нужны). Коммиты `d8c2112` и следующий
- «N/A» в дате рождения — нет данных в источнике: весь 2013 год «Бума» (заглушка 1905), единицы в других годах; всего было 741 заявка
- Восстановление (`scripts/recover_birthdays.py`, коммит `e265b7c`), дата берётся из более поздних заявок человека. Уровни по решению пользователя: A (ФИ + email/телефон) — 204, B1 (ФИ + город) — 97, X (несколько дат, выбрал пользователь; Минаева Полина — 18.12.2011) — 18. Заявки перепривязаны к карточкам клиентов, пустые карточки удалены (262 + 13). Бэкапы `/root/backups/birthdays_*.json`
- Осталось без даты 422 заявки (из них за 2013 год 185): людей больше нет в других заявках или совпадение только по ФИ. Их не трогаем
- Не сделано: у людей из уровня X в других заявках остались даты с опечатками (например, 2024-11-28 у Баузера Сергея). Их можно выровнять отдельно, если понадобится

---

# Задача: дипломы Забега Икс 2026 (5 км + 2 км)

**Статус:** ✅ ГОТОВО (2026-09-24), коммиты `1834238`, `e8ef77e`

- 5 км — диплом по результату, как у прошлых стартов: фон `static/images/diplomas/xtrail/5km/`, плашки x 102–980, y 910–1222 и 1281–1593 (1080×1960). Событие 117 в БД исправлено 10 → 5 км (по решению пользователя, заявок и результатов не было), `db_event_id: 117`
- 2 км — новый режим `DiplomaConfig.participant_only`: без хронометража, диплом по заявке `/diploma/lead/{lead_id}`, только ФИ в верхней плашке, шрифт подбирается под длинные фамилии; кнопка — в колонке «Диплом» стартового списка (колонка видна только при наличии таких дистанций). `db_event_id: 118`
- Проверка: unit 566, integration по дипломам 8, новый JS-тест; скриншоты headless Chrome (длинная и короткая фамилия, 5 км с местами); на проде наборы id дипломов [113, 110, 117, 116, 115] и [118], фоны 200
- Заявки загружены (коммит `bae271a`, `scripts/import_xtrail_2026.py`): 887 шт. (5 км — 580, 2 км — 307). Вебхук на форме этого старта не подключён. «Забег Икс»/«X Trail» → «Х Трейл» теперь в `parse_products()`; 10 км → 5 км; 4 строки без продукта и 26 ФИО — по решению пользователя; из повторов оставлена последняя заявка
- После загрузки (по решению пользователя): у 19 заявок переставлены фамилия и имя, «Тимофеевв» → «Тимофеева» (18 перепривязаны к существующим карточкам клиентов, 1 карточка переименована, 11 опустевших карточек удалены; бэкап `/root/backups/xtrail_names_20260924_210110.json`); удалены возрастные группы Х Трейл 2 км (на 5 км группы придут из Copernico)
- Стартовый список: Забег Икс снова в выборе; запросы и файл баннера — по имени в БД (`KMUtils.eventDbName()`, коммит `053995a`); кнопка диплома под фамилией вместо колонки; баннер однотонный #562872 (`b92b6e4`)

---

# Задача: всё время — в красноярском часовом поясе (Asia/Krasnoyarsk, UTC+7)

**Статус:** ✅ ГОТОВО (2026-09-24), коммит `abbc3bf`. Требование пользователя: «все времена, где бы они ни были, — в красноярском часовом поясе». Пользователь выбрал переключить сразу, до гонки 27.09.

## Ревью
- Пояса источников установлены точно, по времени заявок из вебхука, а не по суточному ритму: сначала я ошибочно решил, что «Дата оплаты» в UTC. Результат: «Дата оплаты» в Tilda — Москва; `Date` в CRM-выгрузке — UTC−7; `sent`/`Payment date` в старых выгрузках и Google — UTC
- Прод, данные (`scripts/fix_leads_timezones.py`, бэкап `/root/backups/leads_tz_fix_20260924_144152.tsv`): «Бум» — 10 248 дат оплаты сдвинуты на +3 ч, 10 561 заглушка стала 01.01 00:00 по Красноярску; Жара 2023–24 — 5281 (+3 ч); Жара 2026 — 2711 строк с датой из `Date` пересчитаны по «Дате оплаты». Ночной 2025 — 829 строк пересчитаны через `--reapply-ids` (бэкап `leads_fix_20260924_144250.tsv`). Не тронуты: Детский 2026 (971) и 794 строки Жары 2026 — у них дата импорта, а не из `Date`
- Прод, сервер: `timedatectl` → Asia/Krasnoyarsk; `/etc/mysql/mysql.conf.d/zz-timezone.cnf` → `default-time-zone='+07:00'`; перезапущены mysql и km_track. `clients.first/last_lead_date` пересчитаны: 20 043, расхождений 0
- Проверка: по красноярскому времени регистраций вебхука меньше всего в 4–7 утра (местная ночь); время выстрела Жары 2026 5 км = 22.08 09:30:03 по Красноярску; страницы 200; unit 564/564; JS-тесты — те же 3 файла, что падают на main
- Вне задачи: большие ответы `/api/event-results` без gzip иногда обрываются на ~94–98 КБ (nginx, в error.log пусто). Браузеры получают gzip — целиком. В бэклог
- Для DataLens: подключение `km_analytic` берёт пояс сервера (+07:00). Проверить в самом DataLens, что даты датасета не сдвигаются ещё раз

## Как сейчас (инвентаризация прода)
- VPS: `Europe/Moscow`. MySQL: `time_zone=SYSTEM`, то есть MSK; таблиц часовых поясов нет. Python `datetime.now()`, `NOW()` и логи идут по MSK
- В БД `TIMESTAMP`: `leads.created_at`, `age_group_configs.created_at`, `participant_photos.created_at`. Хранятся в UTC, при чтении показываются в поясе сессии, то есть по MSK. `DATETIME`: `clients.first_lead_date`/`last_lead_date` — это копии, записанные по MSK
- DataLens (пользователь `km_analytic`) видит время в поясе сервера, то есть по MSK
- Фронт: `toLocaleTimeString()` без `timeZone` в tracker-map (время старта, прогноз финиша), tracker-api, admin-monitoring, duathlon-chart — время показывается по часам браузера зрителя
- `gun_time` в YAML уже явно задан по Красноярску (`results_service`: `timezone(timedelta(hours=7))`)
- cron и systemd-таймеры на VPS только системные (certbot, apt, sysstat), у приложения таймеров нет
- Импорты со сдвигом:
  - Жара 2023–2024: `sent` в UTC записан как MSK, реальное время на 3 ч позже
  - «Бум» 2020–2022: `Payment date` в UTC записан как MSK, на 3 ч позже
  - «Бум» 2013–2022, заглушки 01.01 00:00 MSK: должны стать 01.01 00:00 по Красноярску
  - Жара 2026 и Детский 2026 из админки: колонка `Date` CRM-выгрузки, по данным Ночного 2025 это UTC−10. Проверить на исходных CSV

## План
1. [x] Сервер: `timedatectl set-timezone Asia/Krasnoyarsk`, MySQL `default-time-zone='+07:00'` в my.cnf, перезапуск MySQL и приложения. В России нет перехода на летнее время, поэтому фиксированный `+07:00` точен и не требует таблиц поясов
2. [x] Приложение: пул соединений явно выполняет `SET time_zone='+07:00'`, чтобы не зависеть от my.cnf
3. [x] Данные `TIMESTAMP` мигрировать не нужно: они хранятся в UTC и сами станут показываться по Красноярску. `clients.first/last_lead_date` пересчитать
4. [x] Исправить импорты со сдвигом: Жара 2023–24 и «Бум» 2020–22 (+3 ч к моменту), заглушки → 01.01 00:00 по Красноярску, Жара/Детский 2026 — после проверки CSV. Перед изменениями сделать бэкап
5. [x] Импорт в админке (`bulk_import_leads`): брать «Дату оплаты» (Москва, запасная — `Date`, UTC−7) и переводить в Красноярск. Разовые скрипты работают в явной сессии (UTC или +07:00)
6. [x] Фронт: все `toLocale*String` с `timeZone: 'Asia/Krasnoyarsk'`
7. [x] Проверить: live-трекер (время старта, прогноз финиша), DataLens (даты в датасете), логи, тесты

---

# Задача: заявки Ночного забега 2024–2025 (Tilda CSV + Google xlsx)

**Статус:** ✅ ГОТОВО (2026-09-24), коммит `8b8318a`, скрипт `scripts/import_tilda_google_registrations.py` (универсальный, для остальных стартов 2025)

- 2025: в БД 829 строк загружены одной пачкой (`created_at` 2025-01-21 04:50 UTC, `amount` 0). По решению пользователя им проставлены реальные дата и сумма; строки без даты получили дату соседней строки Google. 19 «недостающих» — те же люди со склеенным в форме ФИО, сопоставлены по email+ДР+дистанции
- 2024: вставлено 929 (те же правила, что у Жары)
- Прод: dry-run совпал с локальным. Исправлено 829, вставлено 929, ошибок 0, осталось строк пачки 0, клиентов-сирот 0. Бэкап: `/root/backups/leads_fix_20260924_141851.tsv`
- **Открыто (часовые пояса):** `sent`/`Payment date` в выгрузках Tilda — UTC. Жара 2023–2024 и «Бум» вставлялись в сессии MSK, поэтому их `created_at` на 3 ч раньше реальных. `Date` в CRM-выгрузке Tilda сдвинута на −10 ч, а `bulk_import_leads()` и бэкфилл Жары 2026 брали как раз `Date`. Нужно решение пользователя

---

# Задача: импорт заявок Жары 2023–2024

**Статус:** ✅ ГОТОВО (2026-09-24), коммит `1136cd7`, скрипт `scripts/import_zhara_2023_2024.py`

- Источник: `Жара_2024-2023.xlsx`, листы 2024 и 2023, старая выгрузка Tilda. Парсер `tilda_import_parser` её не разбирает: `parse_products` не понимает такой `product`, а дата лежит в колонке `sent`
- Решения пользователя: дистанция из SKU; заявки без оплаты импортируются с датой ближайшей строки выше; дубли на одной дистанции схлопываются; даты рождения 991 → 1991, M/D/YY разбирается, невозможные → 1900-01-01
- Вставка вынесена из импорта «Бума» в `insert_leads()`
- Проверка: 21 unit-тест, локально apply + повторный прогон (вставлено 0 → идемпотентно), API стартового списка через TestClient. На проде dry-run совпал с локальным, apply: 5281 строк, 0 ошибок, 0 без события/клиента. Файл с ПДн удалён с VPS
- Итог: 2023 — 2246, 2024 — 3035; `leads` 43 888, `clients` 20 614
- Для локальных скриптов `.env.local` сам не подгружается (`settings` читает только `.env`). Перед запуском нужно экспортировать переменные: `set -a; eval "$(grep -E '^[A-Z_]+=' .env.local | tr -d '\r')"`

---

# Задача: битые clients.last_result_id

**Статус:** ✅ ГОТОВО (2026-09-24), коммит `64dcca0`

- Причина: `trg_results_after_insert` ставит ссылку при вставке, а триггера на DELETE results не было. После перезаливок и чисток результатов ссылка вела на удалённую строку. Код `results.client_id` не меняет, поэтому других путей нет. 1600 ссылок были равны `0`: это заглушка из `trg_results_before_insert` у клиентов, чьи результаты потом удалили
- Фикс: `migrations/add_results_after_delete_trigger.sql` (триггер + разовый пересчёт по MAX(results.id)), `migrations/cleanup_orphan_clients.sql`
- Прод: исправлено 3708 ссылок, удалено 3375 клиентов-сирот (22537 → 19162). После: dangling 0, отличий от MAX 0, сирот 0. Бэкап: `/root/backups/clients_ptr_orphans_20260924_123827.{ptr.tsv,orphans.sql}.gz`
- Локально: триггер проверен в транзакции с откатом (ссылка переезжает на предыдущий результат, у единственного результата становится NULL)

---

# Задача: Удалить старты не из Красмарафона (Первомайский, «Достигая цели», «Красноярский марафон»)

**Статус:** ✅ ГОТОВО (2026-09-24). Решение пользователя — вариант Б: удалить данные из БД и убрать старты из кода. 403 клиента-сироты тоже удалены (по решению пользователя).

## Ревью
- Код: `541d60b`, деплой прошёл, /, /results, /start_list отдают 200 и больше не упоминают удалённые старты. Unit 524/524. 3 JS-теста падают и на main, к этой задаче не относятся (`document.querySelector` в DOM-стабе баннера, siberman merge)
- Прод: пробный прогон с ROLLBACK, затем COMMIT, результаты совпали. Удалено: results 2228, result_segments 18595, leads 1, age_group_configs 10, events 4, clients 403. Итог: results 10882, leads 38607, clients 22537, events 125
- Бэкап: `/root/backups/non_km_events_20260924_122425.sql.gz` (chmod 600, gzip -t ok). Число строк по каждой таблице совпадает с удалённым; clients — все 1884 затронутых в состоянии ДО изменений. Восстановление: `zcat ... | mysql -u root <db>`, это INSERT без DDL и без триггеров
- Замечено вне задачи: у 3708 клиентов `last_result_id` указывает на несуществующий результат. Так было и до удаления (3752), поле в коде не читается. Лоадер `km_race_loader@zhara_21km` всё ещё running, хотя Жара прошла 23.08

Затронутые события: 142, 143 (Первомайский 5 / 21.1), 152 («Достигая цели»), 146 («Красноярский марафон», тестовая запись).
Строки на проде: results 2228, result_segments 18595, leads 1, age_group_configs 10, participant_photos 0.
Клиенты: у 962 `last_result_id` указывает на удаляемый результат; 1 клиент с `first_event_id` в этом наборе; у 403 клиентов после удаления не останется ни заявок, ни результатов.

## Код
- [x] Удалить: config/events/{pervomay,dostigaya_tseli}.yaml, config/copernico/km_{pervomay_5km,pervomay_21km,rzd_5km}_2026.yaml, config/loader/dostigaya_tseli_5km.env, static/map/2026/pervomay_{5,21}.gpx, static/images/diplomas/dostigaya_tseli/, scripts/fix_post_race.py, docs/runbooks/pervomay_2026.md
- [x] Убрать из: templates/krasmarafon/results.html (option), static/js/utils.js (EVENT_NAMES/COLORS), static/js/analytics-results.js (eventYearToIdMap)
- [x] JS-тесты баннера: заменить 'dostigaya_tseli' на старт Красмарафона
- [x] Прогнать unit + JS тесты, проверить /results локально
- [x] Коммит → деплой (ДО удаления данных, чтобы на страницах не остались ссылки на пустые старты)

## Прод (только после подтверждения)
- [x] Остановить/проверить systemd-лоадеры по этим конфигам
- [x] Бэкап mysqldump --where всех удаляемых строк + затронутых clients → /root/backups на VPS
- [x] В транзакции: DELETE result_segments → results → leads (триггер пересчитает клиента) → age_group_configs → events
- [x] Пересчитать clients.last_result_id (MAX(results.id) или NULL), first_event_id/name
- [x] Клиенты-сироты (403) — по решению пользователя
- [x] Проверка: 0 строк по id, /results и /start_list отдают 200, сброс кеша Redis при необходимости

---

# Задача: Импорт исторических регистраций 2013-2022 («Бум.xlsx»)

**Статус:** ✅ ГОТОВО — импортировано на прод 2026-09-23 (20 809 строк, 0 ошибок)

## Итог

`scripts/import_boom_historical.py`, прогон: локальная изолированная БД →
проверка в браузере → прод (dry-run → apply). На проде:
- **20 809** заявок 2013-2022 (было в `leads` ~17.8к, стало 38 607)
- 0 ошибок, 0 неразрешённых `event_id`/`client_id`
- 10 176 уникальных клиентов, у **5 313** записей нашёлся тот же человек
  среди действующих (2025+) — их клиентская история продлилась назад
- пересчитано `first_lead_date`/`last_lead_date` у 9 768 клиентов
- временные файлы (реальные ФИО/телефоны) удалены с VPS

**Важно про объём эффекта:** импорт наполняет `leads` и `clients`
(стартовые списки прошлых лет, клиентская база/LTV/когорты в DataLens).
Страница «История участия» и карточка спортсмена читают таблицу
`results` (финишные времена, там данные только с 2024) — этот импорт их
НЕ затрагивает, в «Буме» времён и мест нет вообще. Для истории участия
нужны финишные протоколы прошлых лет — отдельная задача.

### Поймано по пути (всё исправлено)
1. `event_name` получался «Жара 10 км» вместо «Жара» — дистанция не
   вырезалась из текста, сломало бы связь с существующими событиями
2. `events.event_distance` — `DECIMAL NOT NULL`, поэтому «импорт без
   дистанции» технически невозможен (триггер обязан распарсить число);
   пришлось вернуться к эвристике по возрасту для Детского забега
3. `clients.first_lead_date` садилась по порядку вставки, а не по реальной
   дате — триггер не двигает её назад; добавлен пересчёт после импорта
4. Селектор года на `/start_list` был захардкожен с 2020 — импортированные
   годы не выбирались через UI, опущено до 2013
5. Первый прогон на проде упал на `1040 Too many connections` (ДО записи,
   БД не пострадала): `max_connections=20`, ~17 держит приложение, а
   `get_pooled_connection()` открывает сразу 10 — переключено на одно
   прямое соединение

### Осталось в бэклог
- **Риск прода**: 3 воркера × `pool_size=10` = 30 потенциальных соединений
  при `max_connections=20` — приложение само способно упереться в лимит
  под нагрузкой (тот же класс, что инцидент дня гонки 2026-08-24)
- Финишные протоколы 2013-2023 в `results` — для «Истории участия»
**Дата:** 2026-09-23
**Файл:** `C:\Users\podbo\Downloads\Бум.xlsx`, лист «Все участники», 20 982 строки данных

## Что за файл

Одна вкладка, 19 именованных колонок (`Sport, surname, name, sex, city, club,
birthday, phone, email, product, distance, Total amount, Payment date,
utm_*, Берег`) + 41 пустая колонка без данных (игнорируем). `Sport` = «Бег»
у всех строк — не несёт информации.

Судя по всему — склейка нескольких экспортов Tilda разных лет, вставленных
друг под другом с большими пустыми промежутками (9 «блоков» пустых строк,
самый длинный — 10767 строк подряд). Не мешает парсингу (просто пропускаем
пустые строки), но объясняет неоднородность формата по годам (см. ниже).

## Покрытие: события и годы

| Событие в файле | Годы | Строк | Целевой `event_name` в БД |
|---|---|---|---|
| Жара (10/5/21 км в тексте) | 2013, 2016-2022 | 9077 | `Жара` (без изменений) |
| Детский забег | 2013, 2016-2021 | 3039 | `Детский забег` (без изменений) |
| Xtrail | 2017-2022 | 2574 | **`Х Трейл`** (не «Xtrail» — см. ниже) |
| Женская семерка | 2017-2022 | 1812 | `Женская семерка` (без изменений) |
| Ночной забег | 2019-2022 | 1595 | `Ночной забег` (без изменений) |
| Снежная семерка | 2018-2021 | 1449 | `Снежная семерка` (без изменений) |
| Красочный забег | 2019-2022 | 1072 | `Красочный забег` (без изменений) |
| Забег Весна | 2022 | 364 | **`Весна`** (не «Забег Весна» — см. ниже) |

**Пересечения с текущей БД нет вообще** — в БД самый ранний год по всем этим
событиям 2025. Ноль риска коллизии по годам; риск коллизии по ЛЮДЯМ есть и
желателен (человек из 2018 года мог зарегистрироваться и в 2025 — триггер
БД сам найдёт/создаст `client_id` по surname+name+birthday, обогатит его
историю — в этом и смысл импорта).

## Что нужно сделать (по категориям)

### 1. Нормализация названий событий и дистанций (ОБЯЗАТЕЛЬНО, иначе задвоятся события в БД)

- `event_name`: **`Xtrail` → `Х Трейл`**, **`Забег Весна` → `Весна`** — иначе
  триггер `trg_leads_before_insert` заведёт ВТОРУЮ, отдельную строку в
  `events` вместо привязки к уже существующим (для Xtrail в БД уже есть
  исторические заявки под именем «Х Трейл» — проверено напрямую в БД)
- `event_distance`: привести к формату, который уже использует БД —
  **`"21 км" → "21.1 км"`**, **`"500 метров" → "500 м"`**. Остальные
  (`5 км`, `10 км`, `1 км`, `7 км`, `2 км`) уже совпадают
- Обрезать пробелы (`"Жара 10 км 2019 "` с хвостовым пробелом — 2 строки,
  задвоение с `"Жара 10 км 2019"` без пробела)

### 2. Дистанция не указана НИ В ТЕКСТЕ, НИ ОТДЕЛЬНОЙ КОЛОНКОЙ за старые годы

Колонка `distance` (числом) появляется только с 2020 года — для более
ранних лет узнать дистанцию неоткуда, кроме как из текста `product`
(работает только для «Жара») или общих знаний организатора:

| Событие | Годы БЕЗ дистанции | Дистанция в 2020-2022 (для сравнения) | Предполагаю |
|---|---|---|---|
| Xtrail | 2017-2019 | 10 км (все 3 года) | **10 км**? |
| Женская семерка | 2017-2019 | 7 км (все 3 года) | **7 км**? |
| Снежная семерка | 2018-2019 | 7 км (оба года) | **7 км**? |
| Красочный забег | 2019 | 5 км (все 3 года) | **5 км**? |
| Ночной забег | 2019 | 5 км (все 3 года) | **5 км**? |

✅ **Подтверждено пользователем** — предположения верны (10/7/7/5/5 км).

Отдельно: **«Жара 2020», 75 строк** — нет дистанции ни в тексте, ни в
колонке. ✅ **Пользователь решил: удалить эти 75 строк** (не импортировать).

**Детский забег** — проверил порог по возрасту на реальных данных 2020-2021
(там дистанция уже указана) — **чёткой границы НЕТ**, это была реальная
опция выбора родителем, не строгое правило:

| Возраст | 500 м | 1 км |
|---|---|---|
| 0-3 | 152 | 33 |
| 4 | 75 | 26 |
| 5 | 118 | 24 |
| 6 | 6 | 133 |
| 7-11 | 3 | 751 |

Возраст 4-5 — реально смешанный выбор (⅓ идёт на 1 км). Возрастная эвристика
(`<6 лет → 500 м, ≥6 лет → 1 км`, как уже сделано в `parse_products()` для
текущих загрузок) даст **90%+ точность, но не 100%** — для 2013/2016-2019
(нет отдельной колонки distance) это единственный доступный вариант.
**Предлагаю** так и сделать, с явной пометкой в отчёте импорта, что
дистанция для этих лет — оценка, не точный факт.

**Отдельно, важная находка**: сентинел даты рождения `1905` (см. п.3) —
это НЕ россыпь единичных ошибок, а **систематическая заглушка на весь
год**: 100% строк «Жара 21 км 2013» (159/159), «Жара 10 км 2013» (63/63),
«Детский забег 2013» (37/37) имеют дату рождения 1905 — то есть **в 2013
году реальные даты рождения не собирались вообще**. Для «Детский забег
2013» (37 детей) это значит — дистанцию по возрасту посчитать НЕЛЬЗЯ (нет
даты рождения), только эвристика/дефолт или пометка «неизвестно».
Отдельно `1905` встречается ЕЩЁ и точечно (не по всему году) в «Жара
5/21/10 км 2020» (74 строки) и «Ночной забег 2022» (20 строк) — там это
уже единичные пропуски, не системная проблема года.

### 3. Дата рождения — «заглушки» вместо реальных значений

- **356 строк «Детский забег 2013» с датой рождения `1905-XX-XX`** —
  явная заглушка «неизвестно» (ребёнку было бы 108 лет). Нужно заменить на
  тот же сентинел, что уже используется в проекте для «дата неизвестна»
  (`'1900-01-01'`, см. `bulk_import_leads()`), а не заносить фейковую дату
  в БД как настоящую
- **1 строка**: год рождения `2991` (Колодезная Екатерина, «Забег Весна
  2022») — опечатка. ✅ **Пользователь решил: считать `1991`**
- **2 строки** с датой как ТЕКСТ вместо даты: `'03.07.1881'` (Ручкина
  Елена — тоже похоже на заглушку/ошибку, 145 лет), `'15.05 1986'`
  (Орлова Елена — пробел вместо точки, реальная опечатка того же класса,
  что уже чинили раньше в проекте, `convert_birthday()` уже умеет её
  распознавать)
- **17+14 строк** — дата рождения = год события (2021/2022) для «Детский
  забег» — возможно легитимная категория «до 1 года», не обязательно баг

### 4. Фамилия/Имя — 63 строки, где `surname == name` (не 50, как думал изначально)

Первый проход искал только многословные фамилии (59→50 после проверки);
расширенная проверка (`surname.lower() == name.lower()`, без требования
пробела) нашла ещё 13 однословных случаев той же природы. Итого 63 строки,
**3 разных случая** — разложены по 3 листам файла
`Бум_разбор_ФИО.xlsx` (отправлен пользователю в Downloads):

- ✅ **Лист 1 (50 строк)** — пользователь прислал разбор Фамилия/Имя для
  всех 50 строк (`Бум_разбор_ФИО.xlsx`, лист «1. ФИО задвоено», колонки J/K)
- ✅ **Лист 2 (5 строк)** — **удалить**, не импортировать (surname/name —
  обязательные поля в БД, оставлять половину неизвестной нельзя)
- ✅ **Лист 3 (8 строк)** — пользователь решил: **удалить все 8**, включая
  5 строк `1/1` с реальными email/телефоном (не пытаемся восстановить имя)

Есть и настоящие многословные фамилии БЕЗ бага — не в списке (например
`ДИАС ДЕ КИХАНО БАРБЕРО` / `ДАНИЭЛЬ`, испанская составная фамилия +
отдельное имя, `surname != name`) — их трогать не нужно.

### 5. Явные дубли внутри самого файла

86 уникальных комбинаций (surname+name+birthday+product) встречаются 2-3
раза подряд — 88 «лишних» строк. Похоже на случайно продублированные при
склейке экспортов строки. Нужно дедуплицировать перед импортом (оставить
одну копию).

### 6. Email / телефон — по мелочи, не блокирует

- 747 строк без email, 2 строки с явным мусором вместо email (`"ему 49
  лет - группа 40-49"`, ссылка на vk.com) — на пустое/мусорное подставить
  сентинел `example@mail.ru`, как уже делает `bulk_import_leads()`
- 157 строк без телефона — оставить пустым
- Телефон в основном 11 цифр (формат `7XXXXXXXXXX`) — 17 строк с другой
  длиной (7/9/10/12 цифр) — не блокирует импорт, но не помешает проверить руками

### 7. Пол — тривиальная нормализация

`'Мужчина '`/`'Женщина '` (7+5 строк, хвостовой пробел), `'Женский'` (1
строка, другое слово) → `.strip()` + маппинг на `Мужчина`/`Женщина`.

### 8. `Payment date` → `leads.created_at` — КРИТИЧНО, чтобы не повторить недавний баг

Ровно тот класс проблемы, что чинили 2026-09-10 (см.
`.taskmaster/tasks/lessons.md` — «Импорт обязан сохранять исходные
timestamp'ы»): если не прокинуть `Payment date` в `created_at`, все 21000
исторических записей получат `created_at = момент импорта` и снова
сломают чарты динамики регистраций в DataLens, теперь для 10 лет данных
разом. Формат в основном текстовый ISO (`'2020-08-01 08:43'`, легко
парсится), но 16 строк — сырое число Excel (float/int serial date),
нужна отдельная конвертация.

## План реализации (после ответов на вопросы ниже)

- [ ] 1. Уточнить у организатора: дистанции для лет без данных (п.2),
      природа «Жара 2020» без дистанции, опечатка `2991`→`1991`
- [ ] 2. Ручной проход по 50 строкам с задвоенным ФИО (п.4) — решить
      Фамилию/Имя для каждой (можно приложить список отдельным файлом)
- [ ] 3. Написать скрипт разбора `Бум.xlsx` → нормализованные строки
      (по образцу `tilda_import_parser.py`, но под этот конкретный формат
      колонок — он отличается от боевой выгрузки Tilda)
- [ ] 4. Дедупликация внутри файла (п.5)
- [ ] 5. Матчинг/импорт в БД — переиспользовать матчинг
      `_find_lead_matches()`/логику `bulk_import_leads()` (тот же триггер
      резолвит `client_id`/`event_id` по surname+name+birthday+событие)
- [ ] 6. `created_at` = `Payment date` (с конвертацией сырых Excel-чисел)
- [ ] 7. **Сначала прогнать на локальной изолированной БД** (снимок прода,
      порт 3308, уже настроен — см. `sessions/2026-09-11-localhost-dev-isolation`)
      — полный dry-run, сверка счётчиков/сэмплов, ТОЛЬКО ПОТОМ на проде
- [ ] 8. На проде — тем же путём, что бэкфилл `created_at` (SSH, dry-run →
      показать отчёт → `--apply`)

## Все вопросы закрыты

1. ~~Дистанции для лет без записи~~ — ✅ подтверждены (10/7/7/5/5 км)
2. ~~«Жара 2020» без дистанции~~ — ✅ удалить (75 строк)
3. ~~`2991`→`1991`~~ — ✅ подтверждено
4. ~~Лист 1~~ — ✅ разобрано пользователем (50 строк, применяем как есть)
5. ~~Лист 2~~ — ✅ **удалить**, не импортировать (5 строк)
6. ~~Лист 3~~ — ✅ удалить (8 строк)
7. ~~Детский забег без distance (2013, 2016-2019)~~ — ✅ **вариант 2**:
   импортировать БЕЗ разбивки на 500м/1км — `event_distance` для этих лет
   остаётся неопределённым/общим, не гадаем по возрасту

**Переходим к реализации.**
